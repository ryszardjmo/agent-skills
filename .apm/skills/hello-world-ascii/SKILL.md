---
name: hello-world-ascii
description: Print "Hello, World!" in an ASCII-art style. Use when the user wants a simple hello world banner or terminal-style greeting.
---

# Hello World ASCII

When this skill is used, respond with `Hello, World!` rendered as ASCII art in a fenced `text` code block.

Keep the response focused on the banner itself unless the user asks for variations.
